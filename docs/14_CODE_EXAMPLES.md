# 14 - CODE EXAMPLES

Version: 1.0

---

# هدف

این سند استاندارد نوشتن تمام مثال‌های کدنویسی در مقالات CafeAmoozesh را مشخص می‌کند.

هدف این است که تمام مثال‌ها:

- آموزشی باشند.
- قابل اجرا باشند.
- واقعی باشند.
- خوانا باشند.
- قابل استفاده در پروژه باشند.

---

# اصل طلایی

هر مثال باید یک مفهوم مشخص را آموزش دهد.

از نوشتن مثال‌هایی که چند مفهوم را هم‌زمان آموزش می‌دهند خودداری شود.

---

# ساختار هر مثال

هر Example باید دقیقاً شامل بخش‌های زیر باشد:

1. معرفی مثال
2. کد
3. خروجی
4. تحلیل
5. نکته آموزشی

---

# معرفی مثال

قبل از هر Code Block، هدف مثال توضیح داده شود.

نمونه:

در مثال زیر نحوه اجرای Promise توسط Event Loop را مشاهده می‌کنید.

---

# Code Block

تمام Code Blockها باید مطابق استاندارد زیر باشند.

```html
<pre class="line-numbers">
<code class="language-javascript">

// Code

</code>
</pre>
```

---

# زبان کد

همیشه Language مشخص شود.

نمونه‌ها:

language-html

language-css

language-javascript

language-typescript

language-json

language-bash

language-sql

---

# توضیحات داخل کد

Commentها باید کوتاه باشند.

از توضیح طولانی داخل کد خودداری شود.

---

# نام متغیرها

از نام‌های واقعی استفاده شود.

اشتباه:

```javascript
let a = 10;
let b = 20;
```

صحیح:

```javascript
let productPrice = 10;
let discount = 20;
```

---

# خروجی مثال

بعد از هر مثال، خروجی مورد انتظار نمایش داده شود.

نمونه:

خروجی:

```text
Promise Resolved
```

---

# تحلیل مثال

بعد از خروجی، عملکرد کد مرحله‌به‌مرحله توضیح داده شود.

---

# مثال‌های پروژه واقعی

در صورت امکان مثال‌ها از سناریوهای واقعی انتخاب شوند.

نمونه‌ها:

- دریافت اطلاعات از API
- اعتبارسنجی فرم
- مدیریت کاربران
- سبد خرید
- احراز هویت
- داشبورد مدیریت

---

# ترتیب مثال‌ها

مثال‌ها باید از ساده به پیچیده مرتب شوند.

نمونه:

Example 1

↓

Example 2

↓

Example 3

↓

Real Project

---

# مثال اشتباه

در صورت نیاز ابتدا نمونه اشتباه نمایش داده شود.

سپس نسخه صحیح ارائه گردد.

---

# Common Mistakes

اگر مثال باعث بروز خطاهای رایج می‌شود، بخش «اشتباهات رایج» اضافه شود.

---

# Best Practice

پس از مثال، بهترین روش پیاده‌سازی معرفی شود.

---

# Performance

اگر موضوع به Performance مرتبط است، اثر مثال بر کارایی توضیح داده شود.

---

# Security

در موضوعات امنیتی، خطرات پیاده‌سازی نادرست توضیح داده شود.

---

# Browser Compatibility

در صورت نیاز، سازگاری مثال با مرورگرها یا نسخه‌های زبان ذکر شود.

---

# تعداد مثال‌ها

حداقل:

3 مثال

پیشنهادی:

5 تا 10 مثال

---

# فرمت کد

- تورفتگی یکنواخت (2 یا 4 فاصله)
- بدون Tab و Space مخلوط
- بدون خطوط اضافی

---

# ممنوعیت‌ها

- کد اجرا نشده
- مثال ناقص
- مثال بدون توضیح
- مثال بدون خروجی
- Commentهای طولانی
- نام متغیرهای نامفهوم
- کد بدون Language Class

---

# چک‌لیست

☐ هدف مثال توضیح داده شده است.

☐ Code Block دارای line-numbers است.

☐ Language Class مشخص شده است.

☐ کد بدون خطاست.

☐ خروجی نمایش داده شده است.

☐ تحلیل نوشته شده است.

☐ Best Practice اضافه شده است.

☐ اشتباهات رایج اضافه شده است.

☐ مثال واقعی است.

☐ مثال آماده استفاده در مقاله است.

========================
END OF FILE
========================